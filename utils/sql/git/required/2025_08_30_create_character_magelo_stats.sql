CREATE TABLE IF NOT EXISTS `character_magelo_stats` (
  `id` INT UNSIGNED NOT NULL,
  `weight` INT DEFAULT 0,
  `aa_points_unspent` INT DEFAULT 0,
  `aa_points_spent` INT DEFAULT 0,
  `hp_regen_standing_base` INT DEFAULT 0,
  `hp_regen_sitting_base` INT DEFAULT 0,
  `hp_regen_resting_base` INT DEFAULT 0,
  `hp_regen_standing_total` INT DEFAULT 0,
  `hp_regen_sitting_total` INT DEFAULT 0,
  `hp_regen_resting_total` INT DEFAULT 0,
  `hp_regen_item` INT DEFAULT 0,
  `hp_regen_item_cap` INT DEFAULT 0,
  `hp_regen_aa` INT DEFAULT 0,
  `mana_regen_standing_base` INT DEFAULT 0,
  `mana_regen_sitting_base` INT DEFAULT 0,
  `mana_regen_standing_total` INT DEFAULT 0,
  `mana_regen_sitting_total` INT DEFAULT 0,
  `mana_regen_item` INT DEFAULT 0,
  `mana_regen_item_cap` INT DEFAULT 0,
  `mana_regen_aa` INT DEFAULT 0,
  `hp_max_total` INT DEFAULT 0,
  `hp_max_item` INT DEFAULT 0,
  `mana_max_total` INT DEFAULT 0,
  `mana_max_item` INT DEFAULT 0,
  `ac_total` INT DEFAULT 0,
  `ac_item` INT DEFAULT 0,
  `ac_shield` INT DEFAULT 0,
  `ac_avoidance` INT DEFAULT 0,
  `ac_mitigation` INT DEFAULT 0,
  `atk_total` INT DEFAULT 0,
  `atk_item` INT DEFAULT 0,
  `atk_item_cap` INT DEFAULT 0,
  `atk_offense` INT DEFAULT 0,
  `atk_tohit` INT DEFAULT 0,
  `STR_total` INT DEFAULT 0,
  `STR_base` INT DEFAULT 0,
  `STR_item` INT DEFAULT 0,
  `STR_aa` INT DEFAULT 0,
  `STR_cap` INT DEFAULT 0,
  `STA_total` INT DEFAULT 0,
  `STA_base` INT DEFAULT 0,
  `STA_item` INT DEFAULT 0,
  `STA_aa` INT DEFAULT 0,
  `STA_cap` INT DEFAULT 0,
  `AGI_total` INT DEFAULT 0,
  `AGI_base` INT DEFAULT 0,
  `AGI_item` INT DEFAULT 0,
  `AGI_aa` INT DEFAULT 0,
  `AGI_cap` INT DEFAULT 0,
  `DEX_total` INT DEFAULT 0,
  `DEX_base` INT DEFAULT 0,
  `DEX_item` INT DEFAULT 0,
  `DEX_aa` INT DEFAULT 0,
  `DEX_cap` INT DEFAULT 0,
  `CHA_total` INT DEFAULT 0,
  `CHA_base` INT DEFAULT 0,
  `CHA_item` INT DEFAULT 0,
  `CHA_aa` INT DEFAULT 0,
  `CHA_cap` INT DEFAULT 0,
  `INT_total` INT DEFAULT 0,
  `INT_base` INT DEFAULT 0,
  `INT_item` INT DEFAULT 0,
  `INT_aa` INT DEFAULT 0,
  `INT_cap` INT DEFAULT 0,
  `WIS_total` INT DEFAULT 0,
  `WIS_base` INT DEFAULT 0,
  `WIS_item` INT DEFAULT 0,
  `WIS_aa` INT DEFAULT 0,
  `WIS_cap` INT DEFAULT 0,
  `MR_total` INT DEFAULT 0,
  `MR_item` INT DEFAULT 0,
  `MR_aa` INT DEFAULT 0,
  `MR_cap` INT DEFAULT 0,
  `FR_total` INT DEFAULT 0,
  `FR_item` INT DEFAULT 0,
  `FR_aa` INT DEFAULT 0,
  `FR_cap` INT DEFAULT 0,
  `CR_total` INT DEFAULT 0,
  `CR_item` INT DEFAULT 0,
  `CR_aa` INT DEFAULT 0,
  `CR_cap` INT DEFAULT 0,
  `DR_total` INT DEFAULT 0,
  `DR_item` INT DEFAULT 0,
  `DR_aa` INT DEFAULT 0,
  `DR_cap` INT DEFAULT 0,
  `PR_total` INT DEFAULT 0,
  `PR_item` INT DEFAULT 0,
  `PR_aa` INT DEFAULT 0,
  `PR_cap` INT DEFAULT 0,
  `damage_shield_item` INT DEFAULT 0,
  `haste_item` INT DEFAULT 0,
  PRIMARY KEY (`id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
